# CEDIA Node Reservation Notes

Reserve a GPU node according to the active cluster policy, sync the repository, install dependencies, then run the external validation scripts from the repository root.

The external validation scripts read only the bundled JSON/CSV/PTH artifacts and do not modify training data.

